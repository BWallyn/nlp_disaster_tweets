from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='model to predict which tweets are about real disasters and which one's aren't',
    author='Benjamin Wallyn',
    license='MIT',
)
